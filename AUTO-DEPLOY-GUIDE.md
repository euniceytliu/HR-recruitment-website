# 🚀 Netlify自动部署完整指南

## 第一步：推送代码到GitHub

### 1. 创建GitHub仓库

1. 访问 https://github.com/new
2. 填写信息：
   - **Repository name**: `my-blog`
   - **Description**: "Eunice的个人博客"
   - **Public** ✓ (选择公开)
   - **不要勾选** "Add a README file"
3. 点击 **Create repository**

### 2. 复制仓库地址

创建后会看到类似这样的地址：
```
https://github.com/你的用户名/my-blog.git
```

### 3. 运行推送脚本

打开终端，执行：

```bash
cd "/Users/euniceytliu/Desktop/my first website"
./push-to-github.sh
```

按提示输入GitHub仓库地址，然后等待推送完成。

**注意**：首次推送可能需要输入GitHub用户名和密码（或Personal Access Token）

---

## 第二步：在Netlify连接GitHub仓库

### 方式A：重新创建站点（推荐）

1. 访问 https://app.netlify.com
2. 点击 **"Add new site"** → **"Import an existing project"**
3. 选择 **"GitHub"**
4. 授权Netlify访问GitHub（如果首次使用）
5. 在仓库列表中找到 `my-blog`
6. 点击仓库名称
7. 配置构建设置：
   - **Branch to deploy**: `main`
   - **Build command**: 留空
   - **Publish directory**: `.` 或留空
8. 点击 **"Deploy site"**

### 方式B：更新现有站点

1. 访问 https://app.netlify.com
2. 进入你当前的网站
3. 点击 **"Site settings"**
4. 在左侧菜单找到 **"Build & deploy"**
5. 点击 **"Link repository"**
6. 选择 **"GitHub"**
7. 选择你的仓库 `my-blog`
8. 配置分支为 `main`
9. 点击 **"Link repository"**

---

## 第三步：删除旧的手动部署（可选）

如果你用方式B，旧的手动上传的部署还在，可以：

1. 进入网站的 **"Deploys"** 页面
2. 新的Git部署会自动成为主部署
3. 旧的部署会自动失效

---

## ✅ 完成！自动部署已设置

从现在起，每次你修改代码并推送到GitHub：

```bash
# 修改文件后
git add .
git commit -m "更新内容"
git push

# Netlify会自动检测到推送并重新部署！
```

---

## 🎯 工作流程示例

### 添加新文章

1. 在Supabase控制台添加文章
2. 无需做任何操作，网站自动显示新文章（因为是实时从数据库读取）

### 修改页面样式

1. 修改CSS或HTML文件
2. 提交并推送：
   ```bash
   git add .
   git commit -m "更新样式"
   git push
   ```
3. Netlify自动部署（1-2分钟后生效）

### 查看部署状态

1. 访问 Netlify 控制台
2. 进入你的网站
3. 点击 **"Deploys"** 标签
4. 可以看到每次部署的状态、日志和预览

---

## 📊 部署徽章（可选）

在GitHub仓库的README中添加部署状态徽章：

```markdown
[![Netlify Status](https://api.netlify.com/api/v1/badges/你的站点ID/deploy-status)](https://app.netlify.com/sites/你的站点名称/deploys)
```

---

## ⚠️ 常见问题

### Q: 推送时要求输入密码？

A: GitHub已不再支持密码登录，需要使用Personal Access Token：

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成token并复制
5. 推送时用token代替密码

### Q: Netlify部署失败？

A: 检查：
- 构建命令是否为空
- Publish directory 是否为 `.` 或空
- 仓库分支是否为 `main`

### Q: 想回滚到之前的版本？

A: 在Netlify的Deploys页面，找到之前的部署，点击 "Publish deploy"

---

## 🎉 享受自动部署的便利！

现在你可以专注于内容创作，代码管理和部署都自动化了！
