#!/bin/bash

# 保护调试页面脚本
# 此脚本会将所有调试、测试页面移动到一个受保护的文件夹

echo "🔒 开始保护调试页面..."

# 创建调试文件夹
mkdir -p debug_pages

# 移动所有调试和测试文件
echo "📦 移动调试文件到 debug_pages/ 文件夹..."

# 移动测试文件
mv test-*.html debug_pages/ 2>/dev/null
mv *-debug.html debug_pages/ 2>/dev/null
mv show-*.html debug_pages/ 2>/dev/null
mv check-*.html debug_pages/ 2>/dev/null

# 创建 README
cat > debug_pages/README.md << 'EOF'
# 调试页面

⚠️ **这些页面仅供开发和调试使用，不应部署到生产环境！**

这些页面包含：
- 数据库直接访问
- 敏感信息展示
- 测试工具

## 使用方法

在本地开发时，可以访问这些页面进行调试。

## 安全警告

**切勿**将这些页面部署到公网！

如果需要在生产环境调试，请：
1. 添加密码保护
2. 使用 VPN
3. 限制 IP 访问
EOF

# 创建 .htaccess 保护（Apache）
cat > debug_pages/.htaccess << 'EOF'
# 拒绝所有访问
Order deny,allow
Deny from all
EOF

# 创建 _redirects 保护（Netlify）
cat > debug_pages/_redirects << 'EOF'
/debug_pages/* 404
EOF

echo "✅ 调试页面已移动到 debug_pages/ 文件夹"
echo "✅ 已创建保护文件"
echo ""
echo "📋 下一步："
echo "1. 确保 debug_pages/ 文件夹在 .gitignore 中"
echo "2. 部署时不要上传 debug_pages/ 文件夹"
echo "3. 本地开发时可以继续使用这些文件"
